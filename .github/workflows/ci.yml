name: CI

on:
    push:
        branches: [main]
        paths:
            - "crates/fail2ban-log-parser-core/**"

    pull_request:
        branches: [main]
        paths:
            - "crates/fail2ban-log-parser-core/**"

env:
    CARGO_TERM_COLOR: always

jobs:
    check:
        name: Check
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
              with:
                  toolchain: stable
                  cache: true
            - name: Install make
              uses: ./.github/actions/install-make
              with:
                  os: ubuntu
            - run: make check

    fmt:
        name: Rustfmt
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
              with:
                  toolchain: stable
                  cache: true
                  components: rustfmt
            - name: Install make
              uses: ./.github/actions/install-make
              with:
                  os: ubuntu
            - run: make fmt-check

    clippy:
        name: Clippy
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
              with:
                  toolchain: stable
                  cache: true
                  components: clippy
            - name: Install make
              uses: ./.github/actions/install-make
              with:
                  os: ubuntu
            - run: make clippy

    test:
        name: Test (${{ matrix.os }})
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os: [ubuntu-latest, macos-latest, windows-latest]
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
              with:
                  toolchain: stable
                  cache: true
            - name: Install make
              uses: ./.github/actions/install-make
              with:
                  os: ${{ matrix.os }}
            - run: make test

    bench:
        name: Benchmarks
        runs-on: ubuntu-latest
        permissions:
            contents: write
            pull-requests: write
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
              with:
                  toolchain: stable
                  cache: true
            - name: Install make
              uses: ./.github/actions/install-make
              with:
                  os: ubuntu

            - name: Run sequential benchmarks
              run: cargo bench -p fail2ban-log-parser-core --bench parsing -- --output-format bencher | tee output.txt

            - name: Run parallel benchmarks
              run: cargo bench -p fail2ban-log-parser-core --bench parsing --features parallel -- parallel --output-format bencher >> output.txt

            - name: Store benchmark results
              uses: benchmark-action/github-action-benchmark@v1
              with:
                  name: Parser Benchmarks
                  tool: cargo
                  output-file-path: output.txt
                  benchmark-data-dir-path: .
                  github-token: ${{ secrets.GITHUB_TOKEN }}
                  auto-push: ${{ github.ref == 'refs/heads/main' }}
                  alert-threshold: "105%"
                  comment-on-alert: true
                  fail-on-alert: false
                  alert-comment-cc-users: "@adrianvillanueva997"

    msrv:
        name: MSRV
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
            - uses: actions-rust-lang/setup-rust-toolchain@166cdcfd11aee3cb47222f9ddb555ce30ddb9659 # v1.17.0
              with:
                  toolchain: "1.85.0"
            - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
            - run: cargo check -p fail2ban-log-parser-core --all-features
